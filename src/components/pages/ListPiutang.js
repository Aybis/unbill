import React from 'react';
import { useSelector } from 'react-redux';
import { Layout } from '../includes';

export default function ListDetailPiutang() {
  return <Layout titlePage={'List  Piutang'}></Layout>;
}
